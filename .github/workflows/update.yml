name: Padlet Auto Sync

on:
  schedule:
    # 15分おきに実行
    - cron: '*/15 * * * *'
  workflow_dispatch: # 手動実行ボタン

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: pip install requests

      - name: Run update script
        run: python update_padlet.py

      - name: Commit and Push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
          # ファイルが存在する場合のみ追加（エラー防止）
          if [ -f padlet_export.md ]; then
            git add padlet_export.md
            
            # 変更がある場合のみコミットとプッシュを実行
            if git diff --staged --quiet; then
              echo "No changes to commit"
            else
              git commit -m "chore: update padlet data"
              # 他のジョブとの競合を防ぐため rebase を使用
              git pull --rebase origin main
              git push origin main
            fi
          else
            echo "Error: padlet_export.md not found."
            exit 1
          fi
